Get PIP & VirtualEnv
  sudo easy_install pip
  pip install virtualenv
Set up a virtual environment (run from within web directory)
  virtualenv -p python2.7 --no-site-packages conciergeenv
  add the alias to make this easier (with your path, of course)
    alias concierge="cd /Users/mmoutenot/projects/Concierge/web && source /Users/mmoutenot/projects/Concierge/web/conciergeenv/bin/activate"
Install MySQL
  Create a database name concierge with a user 'concierge' with password
  'concierge' that has privlidges for all operations in the database.
  If you use mac you can:
    brew install mysql
    And use "Sequel Pro" to easily manage it.
Install requirements
  pip install -r requirements.pip
Install GeoData
  https://docs.djangoproject.com/en/dev/ref/contrib/gis/install/#ref-gis-install
Set up tables:
  chmod +x manage.py
  ./manage.py syncdb
  *** South might require some migrations to be run I'll have to check on this
Run server?


